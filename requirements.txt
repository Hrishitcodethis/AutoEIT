# Core (required) — pinned for reproducibility
pandas==3.0.1
openpyxl==3.1.5
thefuzz==0.22.1
python-Levenshtein==0.27.3
scikit-learn==1.8.0

# Neural semantic similarity — optional, upgrades TF-IDF to neural embeddings
# pip install sentence-transformers
# sentence-transformers>=2.2.0

# POS-based content word detection — optional, improves accuracy
# pip install spacy && python -m spacy download es_core_news_sm
# spacy>=3.5.0
