# Core (required)
pandas>=1.5.0
openpyxl>=3.0.10
thefuzz>=0.20.0
python-Levenshtein>=0.21.0

# Semantic similarity — recommended for hybrid 2↔3 adjudication
# pip install sentence-transformers
sentence-transformers>=2.2.0

# POS-based content word detection — optional, improves accuracy
# pip install spacy && python -m spacy download es_core_news_sm
spacy>=3.5.0
